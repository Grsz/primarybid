<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta
      name="application-name"
      content="<%= htmlWebpackPlugin.options.title %>"
      data-version="<%= htmlWebpackPlugin.options.version %>"
    />
    <meta
      name="viewport"
      content="minimum-scale=1, initial-scale=1, width=device-width, shrink-to-fit=no"
    />
    <title>PrimaryBid test</title>
    <script type="text/javascript">
      window.customProps = {
        config: <%= JSON.stringify(htmlWebpackPlugin.options.config) %>,
        version: '<%= htmlWebpackPlugin.options.version %>',
        mode: '<%= htmlWebpackPlugin.options.mode %>'
      };
    </script>
  </head>
  <body>
    <div id="app">
      <div class='initSpinner'>
        <img class='rotate' src="/assets/images/spinner-solid.svg">
      </div>
    </div>
  </body>
</html>
